name: J129 | SIP | Notify Capability Audit

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit-branch-only:
    name: Ejecutar solamente desde Audit
    runs-on: ubuntu-latest
    steps:
      - name: Bloquear ejecucion fuera de Audit
        shell: bash
        run: |
          echo 'Este workflow de laboratorio debe ejecutarse seleccionando la rama Audit.'
          exit 1
