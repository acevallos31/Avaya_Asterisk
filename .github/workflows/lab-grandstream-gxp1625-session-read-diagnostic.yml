name: G10-17B | Issabel Lab | GXP1625 Session Read Diagnostic | Read-Only

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  select-audit-branch:
    name: G10-17B must run from Audit
    runs-on: ubuntu-latest
    steps:
      - name: Require Audit ref
        shell: bash
        run: |
          echo 'Este workflow operativo vive en la rama Audit.'
          echo 'En Run workflow selecciona Branch: Audit y vuelve a ejecutarlo.'
          exit 1
