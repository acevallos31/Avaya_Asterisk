name: G10-19H8H2D | Issabel Lab | Native Accept Header Test

on:
  push:
    branches: [Audit]
    paths:
      - 'deploy/grandstream/prepare-g10-19h8h2d-accept-helper.py'
      - '.github/workflows/lab-grandstream-g10-19h8h2d-accept-header.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Test whether Accept header restores native SID and provisioning path
    runs-on: [self-hosted, Linux, X64, issabel-lab]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Prepare restricted helpers
        shell: bash
        run: |
          set -euo pipefail
          test "$GITHUB_REF_NAME" = 'Audit'
          python3 -B deploy/grandstream/prepare-g10-19h8h2d-accept-helper.py
          python3 -B deploy/grandstream/prepare-g10-19h8h2-helper.py
          python3 -B deploy/grandstream/prepare-g10-19h8h2c-stage-helper.py
          python3 -B deploy/grandstream/prepare-integrated-applyconfig-helper.py
          python3 -B deploy/grandstream/prepare-g10-19h8h2c-output-helper.py
          bash -n deploy/j129/avaya-j129-lab-deploy

      - name: Sync restricted privileged helper
        shell: bash
        run: |
          set -euo pipefail
          OUT="$(sudo -n /usr/local/sbin/avaya-j129-helper-sync)"
          printf '%s\n' "$OUT"
          grep -Fq 'HELPER-SYNC-ALL-PASS' <<<"$OUT"

      - name: Apply temporary Accept, session identity and stage tracing
        shell: bash
        run: |
          set -euo pipefail
          BEFORE="$RUNNER_TEMP/g10-19h8h2d-before.txt"
          sudo -n /usr/local/sbin/avaya-j129-lab-deploy inspect-grandstream-h8h2-session-patch | tee "$BEFORE"
          grep -Fq 'h8h2_patch_state=READY' "$BEFORE"
          sudo -n /usr/local/sbin/avaya-j129-lab-deploy apply-grandstream-h8h2d-accept-patch \
            | tee "$RUNNER_TEMP/g10-19h8h2d-accept-apply.txt"
          sudo -n /usr/local/sbin/avaya-j129-lab-deploy apply-grandstream-h8h2-session-patch \
            | tee "$RUNNER_TEMP/g10-19h8h2d-h8h2-apply.txt"
          sudo -n /usr/local/sbin/avaya-j129-lab-deploy apply-grandstream-h8h2c-stage-trace \
            | tee "$RUNNER_TEMP/g10-19h8h2d-stage-apply.txt"

      - name: Run one native Endpoint Configurator attempt
        shell: bash
        run: |
          set -euo pipefail
          REPORT="$RUNNER_TEMP/g10-19h8h2d-native.txt"
          set +e
          sudo -n /usr/local/sbin/avaya-j129-lab-deploy run-grandstream-integrated-applyconfig | tee "$REPORT"
          HELPER_RC=${PIPESTATUS[0]}
          set -e
          echo "integrated_helper_rc=$HELPER_RC" >> "$REPORT"
          grep -Fq 'h8h2c_stage_trace_seen=YES' "$REPORT"
          echo 'G10-19H8H2D-CAPTURE=PASS' >> "$REPORT"

      - name: Roll back all temporary live-code patches
        if: always()
        shell: bash
        run: |
          set +e
          STAGE_RC=0; H8H2_RC=0; ACCEPT_RC=0
          sudo -n /usr/local/sbin/avaya-j129-lab-deploy rollback-grandstream-h8h2c-stage-trace \
            | tee "$RUNNER_TEMP/g10-19h8h2d-stage-rollback.txt" || STAGE_RC=$?
          sudo -n /usr/local/sbin/avaya-j129-lab-deploy rollback-grandstream-h8h2-session-patch \
            | tee "$RUNNER_TEMP/g10-19h8h2d-h8h2-rollback.txt" || H8H2_RC=$?
          sudo -n /usr/local/sbin/avaya-j129-lab-deploy rollback-grandstream-h8h2d-accept-patch \
            | tee "$RUNNER_TEMP/g10-19h8h2d-accept-rollback.txt" || ACCEPT_RC=$?
          echo "stage_rollback_rc=$STAGE_RC" >> "$GITHUB_STEP_SUMMARY"
          echo "h8h2_rollback_rc=$H8H2_RC" >> "$GITHUB_STEP_SUMMARY"
          echo "accept_rollback_rc=$ACCEPT_RC" >> "$GITHUB_STEP_SUMMARY"
          if [ "$STAGE_RC" -ne 0 ] || [ "$H8H2_RC" -ne 0 ] || [ "$ACCEPT_RC" -ne 0 ]; then exit 1; fi

      - name: Publish sanitized result
        if: always()
        shell: bash
        run: |
          REPORT="$RUNNER_TEMP/g10-19h8h2d-native.txt"
          {
            echo '## G10-19H8H2D — Native Accept header test'
            echo 'tested_delta=Accept_star_slash_star'
            echo 'phone_password_logged=NO'
            echo 'sid_or_cookie_value_logged=NO'
            echo 'submitted_pvalue_payload_logged=NO'
            [ -f "$REPORT" ] && grep -E '^(applyconfig_rc=|grandstream_start_seen=|grandstream_finished_seen=|grandstream_failed_seen=|h8h2c_stage_trace_seen=|G10-19H8H2C-STAGE=|integrated_helper_rc=|G10-19H8H2D-CAPTURE=)' "$REPORT" || true
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload sanitized evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: g10-19h8h2d-${{ github.run_id }}
          path: |
            ${{ runner.temp }}/g10-19h8h2d-before.txt
            ${{ runner.temp }}/g10-19h8h2d-native.txt
            ${{ runner.temp }}/g10-19h8h2d-stage-rollback.txt
            ${{ runner.temp }}/g10-19h8h2d-h8h2-rollback.txt
            ${{ runner.temp }}/g10-19h8h2d-accept-rollback.txt
          if-no-files-found: warn
          retention-days: 30
