name: G10-17K | Issabel Lab | GXP1625 Browser Sequence Matrix | Controlled Test

on:
  workflow_dispatch:
    inputs:
      confirmation:
        description: 'Run this workflow from branch Audit and type RUN-G10-17K'
        required: true
        type: string

permissions:
  contents: read

jobs:
  visibility_only:
    if: github.ref_name != 'Audit'
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'G10-17K must be executed from branch Audit.'
          exit 1
