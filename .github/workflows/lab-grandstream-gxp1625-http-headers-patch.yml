name: G10-15 | Issabel Lab | GXP1625 HTTP Host-Referer Patch | Controlled Apply

on:
  workflow_dispatch:
    inputs:
      action:
        description: 'Acción controlada'
        required: true
        type: choice
        options:
          - inspect
          - apply
          - rollback
        default: inspect
      confirmation:
        description: 'apply: APPLY-G10-15 | rollback: ROLLBACK-G10-15 | inspect: dejar vacío'
        required: false
        type: string

permissions:
  contents: read

jobs:
  grandstream-http-headers-patch:
    name: Grandstream GXP1625 controlled Host-Referer patch
    runs-on: [self-hosted, Linux, X64, issabel-lab]
    timeout-minutes: 10
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify Audit branch and runner
        shell: bash
        run: |
          set -euo pipefail
          test "${GITHUB_REF_NAME}" = 'Audit'
          test "$(id -un)" = 'github-runner'

      - name: Prepare and validate authorized helper source
        shell: bash
        run: |
          set -euo pipefail
          python3 deploy/grandstream/prepare-g10-15-helper.py deploy/j129/avaya-j129-lab-deploy
          bash -n deploy/j129/avaya-j129-lab-deploy
          grep -Fq 'apply-login-headers-patch' deploy/j129/avaya-j129-lab-deploy
          grep -Fq "'Host': self._ip" deploy/j129/avaya-j129-lab-deploy
          grep -Fq "'Referer': 'http://%s/' % self._ip" deploy/j129/avaya-j129-lab-deploy

      - name: Sync authorized lab helper
        shell: bash
        run: |
          set -euo pipefail
          OUT="$(sudo -n /usr/local/sbin/avaya-j129-helper-sync)"
          printf '%s\n' "$OUT"
          grep -Fq 'HELPER-SYNC-PASS name=avaya-j129-lab-deploy' <<<"$OUT"
          sudo -n -l 2>&1 | grep -F '/usr/local/sbin/avaya-j129-lab-deploy' >/dev/null

      - name: Run controlled action
        id: controlled
        shell: bash
        env:
          ACTION: ${{ inputs.action }}
          CONFIRMATION: ${{ inputs.confirmation }}
        run: |
          set -euo pipefail
          REPORT="$RUNNER_TEMP/g10-15-grandstream-http-headers-patch.txt"
          {
            echo '=== G10-15 GRANDSTREAM GXP1625 HTTP HOST-REFERER PATCH ==='
            echo "requested_action=$ACTION"
            echo 'target=/usr/share/issabel/endpoint-classes/class/issabel/vendor/Grandstream.py'
            echo 'db_write=NO'
            echo 'phone_configuration_write=NO'
            echo 'requires_g10_14=YES'
            echo
            case "$ACTION" in
              inspect)
                sudo -n /usr/local/sbin/avaya-j129-lab-deploy inspect-login-headers-patch
                ;;
              apply)
                test "$CONFIRMATION" = 'APPLY-G10-15' || {
                  echo 'ERROR: confirmación requerida: APPLY-G10-15' >&2
                  exit 2
                }
                sudo -n /usr/local/sbin/avaya-j129-lab-deploy apply-login-headers-patch
                sudo -n /usr/local/sbin/avaya-j129-lab-deploy inspect-login-headers-patch
                ;;
              rollback)
                test "$CONFIRMATION" = 'ROLLBACK-G10-15' || {
                  echo 'ERROR: confirmación requerida: ROLLBACK-G10-15' >&2
                  exit 2
                }
                sudo -n /usr/local/sbin/avaya-j129-lab-deploy rollback-login-headers-patch
                sudo -n /usr/local/sbin/avaya-j129-lab-deploy inspect-login-headers-patch
                ;;
              *)
                echo "ERROR: acción no soportada: $ACTION" >&2
                exit 2
                ;;
            esac
            echo
            echo 'G10-15-PASS'
          } | tee "$REPORT"
          grep -Fq 'G10-15-PASS' "$REPORT"
          echo "report=$REPORT" >> "$GITHUB_OUTPUT"

      - name: Job summary
        if: always()
        shell: bash
        run: |
          REPORT="$RUNNER_TEMP/g10-15-grandstream-http-headers-patch.txt"
          {
            echo '## G10-15 Grandstream GXP1625 Host/Referer patch'
            echo
            echo '| Guardrail | Value |'
            echo '|---|---|'
            echo '| PBX | `Issabel LAB` |'
            echo '| Branch | `Audit` |'
            echo '| DB writes | `NONE` |'
            echo '| Phone writes | `NONE` |'
            echo '| Live code target | `Grandstream.py` |'
            echo '| Depends on | `G10-14 username+password patch` |'
            echo '| Privileged path | `avaya-j129-lab-deploy` |'
            echo
            if [ -f "$REPORT" ]; then
              grep -E '^(requested_action|grandstream_py_sha256|username_payload_line_count|host_header_line_count|referer_header_line_count|g10_15_backup_present|headers_patch_state|before_sha256|after_sha256|restored_sha256|GRANDSTREAM-HEADERS-PATCH-|G10-15-PASS)' "$REPORT" | sed 's/^/`/; s/$/`  /'
            fi
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: g10-15-grandstream-http-headers-patch-${{ github.run_id }}
          path: ${{ runner.temp }}/g10-15-grandstream-http-headers-patch.txt
          if-no-files-found: warn
          retention-days: 30
