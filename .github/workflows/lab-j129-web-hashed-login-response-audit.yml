name: Issabel Lab J129 Web Hashed Login Response Audit

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  visibility:
    name: Ejecutar solamente desde Audit
    runs-on: ubuntu-latest
    steps:
      - name: Guard de rama
        shell: bash
        run: |
          echo "Este workflow debe ejecutarse seleccionando la rama Audit."
          test "${GITHUB_REF_NAME}" = "Audit"
