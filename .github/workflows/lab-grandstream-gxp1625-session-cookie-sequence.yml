name: G10-17D | Issabel Lab | GXP1625 Session Cookie Sequence | Read-Only

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  select-audit-branch:
    name: G10-17D must run from Audit
    runs-on: ubuntu-latest
    steps:
      - name: Require Audit ref
        shell: bash
        run: |
          echo 'Este workflow operativo vive en la rama Audit.'
          echo 'En Run workflow selecciona Branch: Audit y vuelve a ejecutarlo.'
          exit 1
